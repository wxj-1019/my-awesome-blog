name: Frontend CI

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/frontend-ci.yml'
  pull_request:
    branches: [main]
    paths:
      - 'frontend/**'

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        # 与生产 Dockerfile (node:20) 对齐，减少双矩阵耗时
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: frontend

      - name: Run linter
        run: npm run lint
        working-directory: frontend

      - name: Run type check
        run: npm run type-check
        working-directory: frontend

      - name: Run tests
        # coverage 阈值已在 jest.config.cjs 放宽；CI 以用例通过为准
        run: npm run test -- --ci --passWithNoTests --coverage --coverageReporters=text-summary
        working-directory: frontend

      - name: Build
        run: npm run build
        working-directory: frontend
        env:
          # CI 构建不依赖线上 API；避免 SSG 拉接口失败
          NEXT_TELEMETRY_DISABLED: '1'
          NEXT_PUBLIC_SITE_URL: http://localhost:3000
          NEXT_PUBLIC_API_BASE_URL: http://localhost:8989/api/v1
          NEXT_PUBLIC_API_URL: http://localhost:8989/api/v1

      - name: Upload coverage to Codecov
        continue-on-error: true
        uses: codecov/codecov-action@v4
        with:
          file: ./frontend/coverage/clover.xml
          flags: frontend
          name: codecov-frontend
