---
description: 博客后台管理系统功能规划方案
alwaysApply: false
enabled: true
updatedAt: 2026-02-14T18:20:41.041Z
provider: 
---

# 后台管理系统方案

## 一、系统概述

基于现有的 25 张数据库表和 26 个 API 端点，设计一个功能完善的后台管理系统。

---

## 二、功能模块规划

### 1. 内容管理（核心模块）

| 子模块 | 功能描述 | 对应数据表----------|------------|
 |
|--------|| **文章管理** | 列表、创建、编辑、删除、发布/下架、置顶、精选、SEO设置 | `articles` |
| **分类管理** | 列表、创建、编辑、删除、排序、图标/颜色设置 | `categories` |
| **标签管理** | 列表、创建、编辑、删除、颜色设置 | `tags` |
| **评论管理** | 列表、审核通过/拒绝、删除、回复 | `comments` |
| **打字机内容** | 列表、创建、编辑、删除、排序、启用/禁用 | `typewriter_contents` |

### 2. 媒体管理

| 子模块 | 功能描述 | 对应数据表 |
|--------|----------|------------|
| **图片管理** | 上传、列表、删除、预览、变体管理 | `images`, `image_variants` |
| **作品集管理** | 列表、创建、编辑、删除、排序、精选 | `portfolios`, `portfolio_images` |

### 3. 用户与权限

| 子模块 | 功能描述 | 对应数据表 |
|--------|----------|------------|
| **用户管理** | 列表、创建、编辑、删除、权限设置、头像管理 | `users` |
| **租户管理** | 列表、创建、编辑、删除、配额设置 | `tenants` |
| **角色权限** | 超级管理员、内容管理员、普通用户 | 基于 `is_superuser` |

### 4. 互动功能

| 子模块 | 功能描述 | 对应数据表 |
|--------|----------|------------|
| **订阅管理** | 列表、导出、激活/禁用、统计 | `subscriptions` |
| **友链管理** | 列表、创建、编辑、删除、排序、点击统计 | `friend_links` |
| **留言管理** | 列表、审核、回复、删除 | `messages` |

### 5. AI 对话模块

| 子模块 | 功能描述 | 对应数据表 |
|--------|----------|------------|
| **对话记录** | 列表、查看详情、统计token使用 | `conversations`, `conversation_messages` |
| **提示词模板** | 列表、创建、编辑、删除、A/B测试、统计 | `prompts` |
| **记忆管理** | 列表、清理过期记忆 | `memories` |

### 6. 展示内容

| 子模块 | 功能描述 | 对应数据表 |
|--------|----------|------------|
| **时间线管理** | 列表、创建、编辑、删除、排序 | `timeline_events` |
| **天气数据** | 查看缓存、手动刷新 | `weather` |

### 7. 系统监控

| 子模块 | 功能描述 | 对应数据表 |
|--------|----------|------------|
| **请求日志** | 访问日志、响应时间、错误追踪 | `request_logs` |
| **审计日志** | 操作记录、用户行为追踪 | `audit_logs` |
| **统计分析** | 文章访问量、用户活跃度、API调用统计 | 聚合统计 |

---

## 三、页面布局建议

```
┌─────────────────────────────────────────────────────────┐
│  顶部导航栏：Logo | 搜索栏 | 通知 | 用户头像下拉菜单    │
├──────────┬──────────────────────────────────────────────┤
│          │                                              │
│  侧边栏   │              主内容区域                       │
│          │                                              │
│ ┌──────┐ │  ┌────────────────────────────────────────┐  │
│ │仪表盘 │ │  │  页面标题 + 面包屑 + 操作按钮         │  │
│ ├──────┤ │  ├────────────────────────────────────────┤  │
│ │内容  │ │  │                                        │  │
│ │ - 文章│ │  │  表格/表单/详情内容                   │  │
│ │ - 分类│ │  │                                        │  │
│ │ - 标签│ │  │                                        │  │
│ │ - 评论│ │  │                                        │  │
│ ├──────┤ │  ├────────────────────────────────────────┤  │
│ │媒体  │ │  │  分页/筛选/批量操作                    │  │
│ │ - 图片│ │  └────────────────────────────────────────┘  │
│ │ - 作品│ │                                              │
│ ├──────┤ │                                              │
│ │用户  │ │                                              │
│ ├──────┤ │                                              │
│ │系统  │ │                                              │
│ └──────┘ │                                              │
└──────────┴──────────────────────────────────────────────┘
```

---

## 四、技术实现建议

### 路由规划
```
/admin                    # 仪表盘/统计
/admin/articles           # 文章管理
/admin/articles/new       # 新建文章
/admin/articles/:id       # 编辑文章
/admin/categories        # 分类管理
/admin/tags              # 标签管理
/admin/comments          # 评论管理
/admin/typewriter        # 打字机内容
/admin/images            # 图片管理
/admin/portfolios        # 作品集管理
/admin/users             # 用户管理
/admin/tenants           # 租户管理
/admin/subscriptions     # 订阅管理
/admin/friend-links      # 友链管理
/admin/messages          # 留言管理
/admin/conversations     # AI对话记录
/admin/prompts           # 提示词模板
/admin/timeline          # 时间线管理
/admin/weather           # 天气数据
/admin/logs/request      # 请求日志
/admin/logs/audit        # 审计日志
/admin/analytics         # 统计分析
/admin/settings          # 系统设置
```

### UI 组件建议
- 使用现有前端项目的 UI 组件库（TDesign）
- 列表页：表格 + 筛选器 + 分页
- 表单页：弹窗或新页面
- 详情页：卡片式展示

---

## 五、优先级排序

### 第一阶段（核心功能）
1. 文章管理（CRUD + 发布/下架）
2. 分类/标签管理
3. 用户管理
4. 仪表盘统计

### 第二阶段（扩展功能）
5. 评论审核
6. 图片/作品集管理
7. 订阅管理
8. 友链管理

### 第三阶段（高级功能）
9. AI 对话管理
10. 日志监控
11. 时间线管理
12. 天气数据

---

## 六、数据统计仪表盘

建议在首页展示：
- 文章总数 / 今日新增
- 用户总数 / 活跃用户
- 总浏览量
- 订阅用户数
- 最近评论
- API 调用统计（可选图表）