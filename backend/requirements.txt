# Core dependencies
fastapi==0.115.6
uvicorn[standard]==0.36.0

# Database
sqlalchemy==2.0.23
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL driver (sync)
asyncpg==0.29.0  # PostgreSQL async driver
pgvector==0.2.5  # PostgreSQL vector extension support

# Data validation
pydantic==2.12.5
pydantic-settings==2.12.0
email-validator==2.1.2

# Authentication and security
# python-jose >= 3.4.0 修复 CVE-2024-33663 / CVE-2024-33664（算法混淆 / JWE DoS）
python-jose[cryptography]==3.5.0
passlib==1.7.4
bcrypt==3.2.0
# python-multipart >= 0.0.18 修复 CVE-2024-24762（ReDoS）/ CVE-2024-53981
python-multipart==0.0.20

# Logging
loguru==0.7.2

# Environment configuration
python-dotenv==1.0.0

# Rate limiting
slowapi==0.1.9

# Monitoring and system info
psutil==5.9.5
gputil==1.4.0

# CORS support (built into FastAPI)
# Optional: SQLite support (built-in, no extra package needed)
# For development only - production should use PostgreSQL

# Image processing
# Pillow >= 10.2.0 修复 CVE-2023-50447（ImageMath.eval RCE）、>= 10.3.0 修复 CVE-2024-28219
Pillow==10.4.0

# File validation and async file operations
puremagic==1.28
aiofiles==23.2.1

# Redis support
redis==5.0.1

# Alibaba Cloud OSS support（保留兼容，实际存储已切换 MinIO）
oss2==2.18.3
# MinIO object storage (S3-compatible)
minio==7.2.8
requests==2.31.0

# LLM Support / LangChain
httpx==0.27.0
tenacity==8.2.3
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0

# Scheduler
apscheduler==3.10.4