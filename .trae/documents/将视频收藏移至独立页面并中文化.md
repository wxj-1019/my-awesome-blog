## 实施计划

### 1. 创建新的视频页面
- 创建 `frontend/src/app/videos/page.tsx`
- 将 VideoView 的内容移至新页面
- 添加页面标题和布局

### 2. 移动组件到共享位置
- 创建 `frontend/src/components/videos/` 目录
- 移动 `VideoCard.tsx` 到 `components/videos/VideoCard.tsx`
- 移动 `AddVideoModal.tsx` 到 `components/videos/AddVideoModal.tsx`
- 更新导入路径

### 3. 移除个人资料页面的视频标签
- 从 `TabNavigation.tsx` 中移除 "我的追剧" 标签
- 从 `ProfilePage.tsx` 中移除 `activeTab` 的 'videos' 类型
- 移除 VideoView 的导入和渲染逻辑

### 4. 中文化所有文本
将 VideoView 中的英文文本翻译为中文：
- "Personal Collection" → "个人收藏"
- "My Watchlist" → "我的追剧"
- "Track your movies..." → "记录您的电影、剧集和动漫之旅..."
- "Watching" → "在看"
- "Completed" → "已完成"
- "Plan" → "计划"
- "Add New" → "添加新收藏"
- "All" → "全部"
- "Movies" → "电影"
- "TV Shows" → "剧集"
- "Anime" → "动漫"
- "Search library..." → "搜索收藏..."
- "No videos found" → "未找到视频"
- "Try adjusting..." → "尝试调整筛选条件..."
- "Clear Filters" → "清除筛选"

### 5. 清理旧文件
- 删除 `profile/components/VideoView.tsx`
- 删除 `profile/components/VideoCard.tsx`
- 删除 `profile/components/AddVideoModal.tsx`