## 修复计划

### 1. 修复 FriendLinks.tsx 语法错误
- 移除第 76 行多余的闭合括号 `))}` 改为 `)}`

### 2. 修复 FriendLinkCard.tsx 样式类
- 将 `bg-glass` 改为 `bg-glass/30` 以匹配设计系统规范
- 添加 `mounted` 状态来避免 hydration 不匹配

### 3. 修复 FriendLinkCard.tsx 的 hydration 问题
- 添加 `mounted` 状态，在组件挂载前不渲染主题相关的内容

### 4. 验证修复
- 确保所有组件正确导入和导出
- 确保没有 TypeScript 类型错误