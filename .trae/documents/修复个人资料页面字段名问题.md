## 修复个人资料页面无法访问的问题

**问题原因**：
- 后端返回的用户数据使用蛇形命名（如 `full_name`）
- 前端期望驼峰命名（如 `fullName`）
- 导致数据不匹配，页面无法正常显示

**解决方案**：
修改后端的 User schema，使用 Pydantic 的 field_serializer 将字段转换为驼峰命名

**修改文件**：
- `backend/app/schemas/user.py` - 添加字段序列化器，将 `full_name` 转换为 `fullName`