/**
 * @deprecated 历史色板文件，请勿扩展。
 * 运行时主题以 CSS 变量为准：
 *   `src/styles/base/variables.css` + `docs/theme-tokens.md`
 *
 * 仅保留类型 re-export，兼容旧 import。
 */
export type { ThemeMode as Theme, ResolvedMode } from '@/lib/theme-config';
